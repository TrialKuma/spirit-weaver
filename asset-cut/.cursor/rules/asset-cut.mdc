---
description: 从游戏截图中抠出各个UI元素（角色、道具、按钮等），自动裁剪并去除背景，输出透明PNG
globs:
  - scripts/extract.py
  - "*.json"
alwaysApply: false
---

# Asset Cut - UI元素抠图

从游戏截图/UI设计图中识别、裁剪并抠出各个独立元素，输出透明背景的PNG文件。

## 前置条件

需要安装 Python 依赖（全局安装一次即可）：

```bash
pip install Pillow rembg
```

## 使用流程

### 1. 用户提供图片

用户提供一张游戏截图或UI设计图，并说明需要抠出哪些元素。

### 2. 分析图片

查看图片，获取尺寸，目视分析各元素位置，确定裁剪坐标（使用比例值 0.0~1.0）。

### 3. 创建 JSON 配置文件

在项目目录下创建配置文件，格式如下：

```json
{
    "src": "源图片的绝对路径",
    "task": "描述性任务名_日期",
    "elements": {
        "元素名1": [left%, top%, right%, bottom%],
        "元素名2": [left%, top%, right%, bottom%]
    }
}
```

坐标为 0.0~1.0 的比例值，格式 `[left, top, right, bottom]`。

### 4. 执行脚本

```bash
python scripts/extract.py --config 配置文件.json
```

也可以命令行覆盖参数：

```bash
python scripts/extract.py --src 图片.png --task 任务名 --config 配置.json
```

### 5. 验证效果

检查输出图片，确认裁剪位置和去背景效果。如不满意，调整配置中的坐标后重新执行。

### 6. 输出结构

```
项目目录\
├── output\<任务名>\    ← 矩形裁剪中间产物（带背景）
└── final\<任务名>\     ← 最终成品（透明背景PNG）
```

## 坐标估算技巧

- 坐标使用比例值 (0.0~1.0)，格式为 `[left, top, right, bottom]`
- 先从图片视觉上估计元素占图片宽/高的比例
- 第一次裁剪可以稍微放大范围，确认后再收紧
- 重叠元素需要分别裁剪，可能包含其他元素的部分

## 去背景说明

- 使用 rembg (u2net 模型) 进行 AI 去背景
- 独立清晰的元素效果最好
- 重叠密集的区域效果可能不理想
- 如果 rembg 不可用，会自动回退为矩形裁剪输出

## 命名规范

| 类别 | 示例 |
|------|------|
| 角色 | 顾客1_红胡子, NPC_商人 |
| 道具 | 鱼, 丸子串, 宝剑 |
| UI控件 | 返回按钮, 金币图标 |
| 场景物件 | 烤炉_左, 桌子 |
